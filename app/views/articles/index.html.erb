

<% @articles.each do |article| %>
	<%= render partial: "article", locals: {article: article} %>
<% end %>

<%= link_to raw('<i class="icon-pencil icon-white"></i> New Article'), new_article_path, :class => 'btn btn-primary' %>